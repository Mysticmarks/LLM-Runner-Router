apiVersion: v1
kind: Namespace
metadata:
  name: llm-systems
  labels:
    name: llm-systems
    app: llm-runner-router
    tier: backend
    environment: production
  annotations:
    description: "Namespace for LLM Runner Router system"
    contact: "devops@yourdomain.com"
    cost-center: "ai-infrastructure"
spec:
  finalizers:
  - kubernetes